---
phase: 01-download-voltage-level
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/TreeNode/treeNavigation.vue
  - src/views/TreeNode/components/TreeToolbar.vue
  - src/views/TreeNode/Server/mixin/Download/downloadNode.js
autonomous: true
requirements:
  - DVL-01
  - DVL-02
  - DVL-03
  - DVL-04
  - DVL-05
  - DVL-06

must_haves:
  truths:
    - User can download a voltage level node from ServerTree to ClientTree
    - Download includes full ancestor chain (Organisation → Substation → VoltageLevel)
    - If node exists in ClientTree, it is overwritten with server data
    - User can trigger download from toolbar button when node is selected
    - After download completes, a success or error message is displayed
    - ClientTree automatically refreshes to show newly downloaded nodes
  artifacts:
    - path: "src/views/TreeNode/Server/mixin/Download/downloadNode.js"
      provides: "Download logic for all node types"
      contains: "handleDownloadNode"
    - path: "src/views/TreeNode/components/TreeToolbar.vue"
      provides: "Toolbar with download button"
      contains: "@download"
    - path: "src/views/TreeNode/treeNavigation.vue"
      provides: "Event wiring from toolbar to download handler"
      contains: "@download=\"handleDownloadNode\""
  key_links:
    - from: "TreeToolbar.vue"
      to: "treeNavigation.vue"
      via: "@download event"
      pattern: "@download=\"handleDownloadNode\""
    - from: "treeNavigation.vue"
      to: "downloadNode.js mixin"
      via: "handleDownloadNode method"
      pattern: "handleDownloadNode"
---

<objective>
Verify the complete download voltage level workflow is properly implemented and all components are wired together.

Purpose: Ensure all 6 requirements (DVL-01 through DVL-06) are correctly connected and functional.
Output: Verified download workflow with toolbar trigger
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md

# Existing Implementation
The download functionality is already implemented. This plan verifies all connections.

From downloadNode.js:
- handleDownloadNode() - Main handler triggered by toolbar
- Downloads voltage level with full ancestor chain
- Uses ON CONFLICT DO UPDATE for overwrite behavior
- Shows success/error messages via this.$message
- Refreshes ClientTree via showLocationRoot()

From TreeToolbar.vue:
- Download button at line 129: <i @click="handleDownload" title="Download">
- Emits 'download' event to parent

From treeNavigation.vue:
- Line 15 wires: @download="handleDownloadNode"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify toolbar download button is properly wired</name>
  <files>src/views/TreeNode/components/TreeToolbar.vue</files>
  <action>
    Verify the download button in TreeToolbar.vue emits the 'download' event:
    - Line 129 has: &lt;i @click="handleDownload" title="Download"&gt;
    - Line 194-196 has: handleDownload() { this.$emit('download') }
    
    This is already correctly implemented.
  </action>
  <verify>
    <automated>grep -n "@download\|handleDownload" src/views/TreeNode/treeNavigation.vue | head -5</automated>
  </verify>
  <done>Toolbar button emits 'download' event</done>
</task>

<task type="auto">
  <name>Task 2: Verify treeNavigation.vue wires download event to handler</name>
  <files>src/views/TreeNode/treeNavigation.vue</files>
  <action>
    Verify treeNavigation.vue connects the toolbar's @download event to handleDownloadNode:
    - Line 15: @download="handleDownloadNode"
    - The component imports the downloadNode mixin which provides handleDownloadNode
    
    This is already correctly implemented.
  </action>
  <verify>
    <automated>grep -n "downloadNode\|mixins" src/views/TreeNode/treeNavigation.vue | head -10</automated>
  </verify>
  <done>download event wired to handleDownloadNode handler</done>
</task>

<task type="auto">
  <name>Task 3: Verify downloadNode.js implements all DVL requirements</name>
  <files>src/views/TreeNode/Server/mixin/Download/downloadNode.js</files>
  <action>
    Verify downloadNode.js implements:
    - DVL-01: handleDownloadNode() downloads voltage level (line 158-288)
    - DVL-02: Full ancestor chain download (lines 193-229 for org chain, 231-260 for substation)
    - DVL-03: Overwrite via ON CONFLICT DO UPDATE in insert functions
    - DVL-05: Success/error messages (this.$message.success/error throughout)
    - DVL-6: ClientTree refresh (await this.showLocationRoot() at line 51)
    
    All requirements are already implemented in this file.
  </action>
  <verify>
    <automated>grep -n "showLocationRoot\|\$message\|voltageLevel" src/views/TreeNode/Server/mixin/Download/downloadNode.js | head -15</automated>
  </verify>
  <done>All 6 DVL requirements implemented in downloadNode.js</done>
</task>

</tasks>

<verification>
All components are properly wired:
1. TreeToolbar.vue has download button emitting 'download' event
2. treeNavigation.vue wires @download to handleDownloadNode
3. downloadNode.js implements all DVL-01 through DVL-06 requirements
4. The feature is ready for end-to-end testing
</verification>

<success_criteria>
- DVL-01: Download voltage level from ServerTree to ClientTree
- DVL-02: Full ancestor chain (Organisation → Substation → VoltageLevel) 
- DVL-03: Overwrite existing nodes in ClientTree
- DVL-04: Toolbar button triggers download (verified above)
- DVL-05: Success/error messages displayed
- DVL-06: ClientTree refreshes after download

All 6 requirements are verified as implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-download-voltage-level/01-01-SUMMARY.md`
</output>
